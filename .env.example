# Copy to .env to override defaults. Defaults work out of the box with docker-compose.
PORT=4000
DATABASE_URL=postgres://ledn:ledn@localhost:5432/ledn_token
# Per-candidate identifier. Each candidate receives a unique value; it seeds the
# synthetic dataset and derives the challenge flags. Leave as "demo" for local trials.
CANDIDATE_ID=demo
